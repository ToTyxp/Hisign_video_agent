# 项目内 YouTube 视频下载与核验工具：实施计划

## 目标

在当前项目中创建普通本地工具。Codex 负责把自然语言转换为检索与验收条件，`yt-dlp` 负责搜索、探测和下载，FFmpeg/ffprobe 负责文件核验。整个流程不依赖 MCP，也不部署或下载本地 ASR 模型。

## 工作流

1. Codex 先识别原始输入语言，对“异常行为”做路由并选用对应的行为模板，再生成中文 + English 双语搜索方案；将英文规范意图写入 `translation`，后续搜索词、内容条件和核验要求统一使用英文。
2. 使用 yt-dlp 搜索、去重、探测元数据并收集可用字幕。
3. 由 Codex 对候选给出置信度；低于 0.75 的候选不下载。
4. 默认下载最高 1080p、无损合并为 MKV，并保留经脱敏的元数据、描述、字幕、缩略图和章节；默认单文件 2 GB、单任务总量 4 GB。
5. 对下载结果进行 ffprobe、流、时长及首/中/尾解码检查；存在画面要求时提取关键帧。
6. 使用字幕、章节、描述和关键帧进行语义判断；字幕不足时可显式调用外部 ASR API，不使用本地模型。
7. 只有技术核验通过、硬约束满足且语义分数至少 0.80 的视频会被接受；失败候选自动尝试下一项，最多 3 项。

## 安全与边界

- 固定使用 `yt-dlp --ignore-config --no-plugin-dirs`，不允许任意额外参数或 `--exec`。
- `YTB_Download/`（或用户指定的项目内输出目录）只放最终接受的视频；下载任务、元数据、字幕、关键帧、报告和状态记录全部进入隐藏的 `.ytb-download/`。
- Cookie 仅能通过显式环境变量提供，日志与报告不记录敏感值。
- 外部 ASR 只有在用户明确选择并配置端点、API key 与模型后才调用；密钥不落盘。
- 所有成功下载的视频都会立即写入项目级去重历史；即使验证后拒绝、清理或手动删除文件，也不会被自动重复下载。
- 仅下载用户有权访问及使用的内容，不规避 DRM 或访问限制。

## 验收

- 工具位于项目根目录 `youtube-video-agent/`，仅通过本地脚本调用，不注册为全局或隐式 Codex Skill。
- 缺少依赖时只输出可执行的安装建议，不自动修改系统。
- 每个已接受视频在可见目录只保留可读名称的视频文件；源 `.info.json`、本地探测结果、核验 JSON 与 Markdown 报告保留在隐藏内部目录以供 Codex 核验。
- 低置信度搜索不会产生下载文件；错误候选不会与已接受视频混放。

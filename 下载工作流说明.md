# 下载工作流说明（YouTube 视频批量下载）

本文档整理自本次实际执行的项目：`/Users/yangxp/Desktop/Work_hisign/ytb异常视频下载`。目标是使用 `youtube-video-agent` 插件/技能，从 YouTube 批量下载“打架斗殴 / CCTV 监控 / 小规模举牌抗议”等相关公开视频，并按分类整理到 `YTB_Download/` 下。

---

## 1. 总体目标

本次任务从最初“下载 50 个打架斗殴视频”逐步扩展为 4 个分类，每个分类目标 200 个有效视频：

| 分类 | 输出目录 | 目标 |
|---|---|---|
| 小规模举牌抗议（动物保护、法轮功） | `YTB_Download/batch-protest-small-animal-falun-200/` | 200 |
| CCTV 打架（监控，不是电视） | `YTB_Download/batch-cctv-fight-200/` | 200 |
| CCTV 打架相似事件（对峙、跳舞等） | `YTB_Download/batch-cctv-fight-similar-200/` | 200 |
| 小规模举牌抗议相似事件 | `YTB_Download/batch-protest-small-similar-200/` | 200 |

最终实际完成：

| 分类 | 最终数量 |
|---|---:|
| `batch-protest-small-animal-falun-200` | 229 |
| `batch-cctv-fight-200` | 201 |
| `batch-cctv-fight-similar-200` | 209 |
| `batch-protest-small-similar-200` | 206 |

---

## 2. 插件是什么

插件名：`dsh-youtube-video-agent`  
本质：一个 **DSH（DeepSeek Harness）插件包**，把已有的确定性 YouTube 下载工具封装成可供智能体调用的技能。

插件在项目中的位置：

```text
.dsh-home-test/profiles/web/node_modules/dsh-youtube-video-agent/
├── index.js
├── cordis.patch.yml
├── package.json
├── skill.md
├── README.md
├── prompts/
│   ├── search-intent-analyst.md
│   └── behaviors/
│       ├── fight-brawl.md
│       └── protest-demonstration.md
├── references/
│   └── schemas.md
└── scripts/
    ├── video_agent.py
    └── parallel_batch.py
```

另外项目根目录还有一份可独立运行的副本：

```text
youtube-video-agent/
├── scripts/video_agent.py
├── scripts/parallel_batch.py
└── prompts/...
```

---

## 3. 插件是如何“运作和交互”的

### 3.1 插件注册到 DSH

`cordis.patch.yml` 把插件插入 DSH profile：

```yaml
- insert:
    - id: youtube-video-agent-skill
      name: dsh-youtube-video-agent
```

`index.js` 是插件入口，它向 DSH 的 skill 注册表注册一个技能：

```js
export const inject = ['skills']

export function apply(ctx) {
  const content = readFileSync(join(pluginRoot, 'skill.md'), 'utf8')
  ctx.skills.register({
    name: 'youtube-video-agent',
    description: '...',
    whenToUse: '...',
    source: 'bundled',
    provider: 'dsh-youtube-video-agent',
    resourceBase: { kind: 'directory', path: pluginRoot },
    content,
  })
}
```

关键点：
- 插件本身**不实现下载逻辑**。
- 它只是把 `skill.md` 注入到模型可读的技能库中。
- 真正的下载由 `scripts/video_agent.py` 完成。

### 3.2 智能体与插件交互流程

1. 用户在对话中提出需求，例如“下载 50 个打架斗殴视频”。
2. 智能体看到已注册的 `youtube-video-agent` 技能。
3. 智能体读取 `skill.md`，按照其中的“行为路由”规则：
   - 先判断属于哪类行为；
   - 再读取对应 `prompts/behaviors/*.md`；
   - 生成中英双语搜索方案。
4. 智能体把方案写入 `intent.json`。
5. 智能体调用 `video_agent.py` 的 CLI 完成确定性操作：
   - `preflight`：检查 yt-dlp / ffmpeg / ffprobe / deno；
   - `init-job`：创建内部任务目录；
   - `discover`：搜索、去重、探测元数据、拉取字幕；
   - `download`：按 `selection.json` 下载；
   - `publish`：把视频移动到 `YTB_Download/`。
6. 智能体读取输出文件（`candidates.json`、日志、下载清单），判断是否继续调整查询、筛选条件或技能文档。
7. 最终向用户汇报视频 ID、保存路径和命中理由。

### 3.3 插件内部“运动”路径

可以用下面这条链路表示：

```text
用户请求
   ↓
智能体读取 skill.md
   ↓
行为路由（search-intent-analyst.md + behaviors/*.md）
   ↓
生成 intent.json（英文查询、约束、核验要求）
   ↓
video_agent.py init-job
   ↓
video_agent.py discover
   ↓
生成 candidates.json
   ↓
智能体/auto-select 生成 selection.json
   ↓
video_agent.py download
   ↓
video_agent.py publish
   ↓
YTB_Download/<分类目录>/视频文件
```

### 3.4 批量下载时如何交互

对于大批量任务，智能体不手动一个个下载，而是使用 `parallel_batch.py`：

```text
智能体
   ↓
parallel_batch.py --intent xxx.json --output-dir YTB_Download/xxx
   ↓
按 workers 数量拆分为多个 worker
   ↓
每个 worker 独立执行：
   init-job → discover → 自动选择 → download → publish
   ↓
把结果返回给智能体
```

本次为了避免触发 YouTube 机器人检测/过载，实际使用的是**串行模式**：

```text
run_serial_downloads_roundN.py
   ↓
按分类依次执行
   ↓
每个分类内逐批执行（每批一个 intent，6 个搜索词，目标约 20 个）
   ↓
每批之间 sleep 20~30 秒
   ↓
写日志到 .ytb-download/tmp/serial-download-roundN.log
```

---

## 4. 核心脚本说明

### 4.1 `video_agent.py`

确定性执行器，负责所有本地/网络下载操作。

常用子命令：

```bash
python3 youtube-video-agent/scripts/video_agent.py preflight
python3 youtube-video-agent/scripts/video_agent.py init-job --label <任务名> --output-dir <输出目录>
python3 youtube-video-agent/scripts/video_agent.py discover --intent <intent.json> --job-dir <job_dir> ...
python3 youtube-video-agent/scripts/video_agent.py download --intent <intent.json> --selection <selection.json> --job-dir <job_dir>
python3 youtube-video-agent/scripts/video_agent.py publish --job-dir <job_dir>
```

职责：
- 构造安全的 yt-dlp 参数；
- 维护 `.ytb-download/state/download-history.json` 去重；
- 缓存探测结果到 `.ytb-download/cache/`；
- 把最终视频发布到 `YTB_Download/`。

### 4.2 `parallel_batch.py`

批量协调器，把一个大的 intent 拆给多个 worker 执行。

关键参数：

```bash
python3 youtube-video-agent/scripts/parallel_batch.py \
  --intent <intent.json> \
  --script youtube-video-agent/scripts/video_agent.py \
  --output-dir <输出目录> \
  --workers 1 \
  --probe-limit 40 \
  --caption-limit 5 \
  --per-query 15 \
  --max-candidates 80 \
  --timeout 3600
```

`--workers 1` 表示串行；`--workers 4` 表示并行 4 个 worker。

### 4.3 串行协调脚本

本次实际使用了很多轮次脚本，例如：

```text
.ytb-download/tmp/run_serial_downloads_round10.py
.ytb-download/tmp/run_serial_downloads_round11.py
.ytb-download/tmp/run_serial_downloads_round12.py
.ytb-download/tmp/run_serial_downloads_round13.py
.ytb-download/tmp/run_serial_downloads_round14.py
.ytb-download/tmp/run_serial_downloads_round15.py
.ytb-download/tmp/run_serial_downloads_round16.py
.ytb-download/tmp/run_serial_downloads_round17.py
```

这些脚本的作用：
- 按分类循环；
- 每轮使用新的 `intent-*.json`（新的搜索词）；
- 每批调用一次 `parallel_batch.py`；
- 记录当前文件数、下载数、失败信息；
- 达到目标后自动停止。

---

## 5. 数据与目录约定

### 5.1 用户可见目录

```text
YTB_Download/
├── batch-protest-small-animal-falun-200/
├── batch-cctv-fight-200/
├── batch-cctv-fight-similar-200/
└── batch-protest-small-similar-200/
```

只放最终视频文件，文件名为：

```text
视频标题 [YouTubeID].mp4/.mkv
```

### 5.2 内部隐藏目录

```text
.ytb-download/
├── cache/                  # 探测/字幕缓存
├── jobs/                   # 每个 init-job 的任务目录
│   ├── <job-id>/
│   │   ├── candidates.json
│   │   ├── selection.json
│   │   ├── download-manifest.json
│   │   └── media/
├── state/
│   ├── download-history.json   # 去重历史
│   └── accepted.archive
└── tmp/                    # 轮次脚本、日志、intent 文件
```

### 5.3 去重机制

- 只要成功下载过的视频 ID，都会写入 `.ytb-download/state/download-history.json`。
- 后续 `discover` / `select_auto` 会跳过历史中的 ID。
- 即使文件被删除，去重记录仍保留，除非用户明确要求 `--allow-re-download`。

---

## 6. 搜索意图与筛选

### 6.1 intent.json 结构

```json
{
  "version": 1,
  "translation": {
    "source_language": "zh",
    "original_request": "继续下载 cctv-fight-200 类视频",
    "target_language": "en",
    "canonical_request": "Continue downloading more valid CCTV fight videos"
  },
  "mode": "search",
  "search_queries": ["CCTV fight caught on camera", "..."],
  "count": 20,
  "constraints": {
    "include": ["fight", "brawl", "打架", "斗殴"],
    "exclude": ["news", "vlog", "prank", "dashcam", "..."],
    "duration_min_seconds": 10,
    "duration_max_seconds": 1800
  },
  "verification": {
    "semantic_requirements": ["The video matches the requested behavior category and is clearly visible."]
  }
}
```

### 6.2 质量门槛

根据实际标注反馈，插件在 `skill.md` 和 `prompts/behaviors/*.md` 中维护了质量门槛，以下情况直接拒绝：

- 新闻、演播室、记者旁白、新闻角标；
- 镜头太乱、太晃、频繁切换；
- 事件窗口太短；
- 模糊、马赛克、跳帧、加速/慢动作；
- 解说字幕、水印、图形遮挡；
- 表演、摆拍、恶搞、vlog；
- 车载摄像头、手机竖屏、非监控/非现场记录。

---

## 7. 本次执行中的关键调整

1. **Cookie 路径修正**
   - 现象：`YOUTUBE_VIDEO_AGENT_COOKIES_FILE` 指向不存在文件。
   - 修正：指向项目根目录 `www.youtube.com_cookies.txt`。

2. **HTTP 403 Forbidden**
   - 现象：搜索/探测正常，但下载报 403。
   - 修正：在 `video_agent.py` 的 yt-dlp 参数中加入：
     ```text
     --extractor-args youtube:player_client=android
     ```

3. **自动选择误杀**
   - 现象：描述里出现 `news`、`report` 等泛化词时，有效视频被误杀。
   - 修正：只对“标题 + 频道名”做新闻/vlog 等红词过滤，不再因描述泛化词排除。

4. **串行代替并行**
   - 现象：并行下载可能导致 YouTube 机器人检测/过载。
   - 修正：使用 `--workers 1` 串行执行，每批之间 sleep。

5. **代理/VPN 问题**
   - 现象：yt-dlp 走 `127.0.0.1:7897` 代理，代理未启动时搜索/下载全部失败。
   - 修正：用户打开代理后重新运行对应轮次，下载恢复。

---

## 8. 日志与进度

主要日志文件：

```text
.ytb-download/tmp/serial-download-roundN.log
.ytb-download/tmp/serial-download-roundN-console.log
.ytb-download/tmp/progress-reports.log
```

进度报告格式示例：

```text
[2026-08-24T11:18:41] REPORT total=829 (milestone 820) | ...=229 | ...=185 | ...=209 | ...=206
```

---

## 9. 如何重新运行

如果你以后要重新跑类似任务：

```bash
# 1. 检查依赖
python3 youtube-video-agent/scripts/video_agent.py preflight

# 2. 准备 intent.json（每个文件最多 6 个 search_queries，exclude 最多 20 项）

# 3. 串行批量下载
nohup .venv/bin/python .ytb-download/tmp/run_serial_downloads_roundN.py \
  < /dev/null > .ytb-download/tmp/serial-download-roundN-console.log 2>&1 &
```

注意：
- 每个 `intent.json` 的 `search_queries` 不能超过 6 个；
- `constraints.exclude` 不能超过 20 项；
- 确保代理/VPN 已打开；
- 大批量任务建议串行，避免触发 YouTube 风控。

---

## 10. 完成状态

最终所有分类均已达到目标：

| 分类 | 数量 |
|---|---:|
| 小规模举牌抗议（动物保护/法轮功） | 229 |
| CCTV 打架（监控） | 201 |
| CCTV 打架相似事件 | 209 |
| 小规模举牌抗议相似事件 | 206 |

下载工作流已完整跑通。
